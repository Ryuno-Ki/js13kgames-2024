/**
 * Helper function to sanitise user input by removing potentially dangerous characters.
 *
 * @argument {string} rawString
 * @returns {string}
 */
export function sanitise(rawString: string): string;
