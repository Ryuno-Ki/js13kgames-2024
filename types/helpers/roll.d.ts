/**
 * Randomly generate a number between lower and upper.
 *
 * @argument {function} rng
 * @argument {number} upper
 * @argument {number} lower
 * @returns {number}
 */
export function roll(rng: Function, upper: number, lower?: number): number;
