/**
 * Helper function to randomly pick an item from a list.
 *
 * @argument {Array<string>} list
 * @argument {function} rng
 * @returns {string}
 */
export function pickRandom(list: Array<string>, rng: Function): string;
