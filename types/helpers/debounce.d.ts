/**
 * Helper function to debounce a function by a delay.
 *
 * @argument {function} fn
 * @argument {number} [delay=300]
 * @returns {function}
 */
export function debounce(fn: Function, delay?: number | undefined): Function;
